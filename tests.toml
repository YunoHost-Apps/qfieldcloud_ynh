test_format = 1.0

[default]

    # ------------
    # Tests to run
    # ------------

    preinstall = """
    sudo yunohost domain add minio.domain.tld
    sudo yunohost user create minio_admin
    """

    args.minio_admin="minio_admin"
